<div class="font-b">
  <div class="fort-title">
    <h1>Vos résevervations</h1>
  </div>


  <div class="cards">
    <% @bookings.each do |booking| %>
      <div class="card-trip">
        <%= image_tag booking.item.poster_url %>
        <div class="card-trip-infos">
          <div>
            <h2><%= booking.item.name%></h2>
            <p><%= booking.status%></p>
          </div>
          <h2 class="card-trip-pricing"><%= booking.item.price %></h2>
        </div>
         <% end %>
      </div>
   </div>








</div>
