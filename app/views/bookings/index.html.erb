<div class="font-b">
  <div class="fort-title">
    <h1>Vos réservations</h1>
  </div>





<div class="container">
  <div class="cards">
    <% @bookings.each do |booking| %>
      <div class="card-category">
        <%= image_tag booking.item.poster_url, class:"card-image", alt: "" %>
        <div class="card-category-info">
          <h2><%= booking.item.name %></h2>
           <p><%= booking.start_at.strftime('%-d %b. %y') %> au <%= booking.end_at.strftime('%-d %b. %y') %></p>
            <p><%= booking.status %></p>

          <%# <% if item.user.present? %>
            <%# <%= image_tag item.user.photo_url, class: "card-category-user" %>
          <%# <% else %>
          <%# <p>Aucune photo d'utilisateur disponible</p> %>
          <%# <% end %>

             <img src="<%= booking.item.user.photo_url %>" class="avatar">

        </div>
      </div>
    <% end %>
  </div>
</div>
</div>





</div>
