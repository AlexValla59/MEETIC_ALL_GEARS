<div class="main-image">
  <div class= "title-index">
    <h1>Liste d'objets</h1>
  </div>


<div class="container">
  <div class="cards">
    <% @items.each do |item| %>
      <div class="card-category">
        <%= image_tag item.poster_url, class:"card-image", alt: "" %>
        <div class="card-category-info">
          <h2><%= item.name %></h2>
          <p><%=item.price %>€</p>
          <%= link_to "infos", item_path(item), class: "button-info" %>

          <%# <% if item.user.present? %>
            <%# <%= image_tag item.user.photo_url, class: "card-category-user" %>
          <%# <% else %>
          <%# <p>Aucune photo d'utilisateur disponible</p> %>
          <%# <% end %>

          <img src="<%=item.last.user.photo_url %>" class="avatar">

        </div>
      </div>
    <% end %>
  </div>
</div>
</div>
